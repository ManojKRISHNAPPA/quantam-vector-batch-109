pipeline {
  agent any 

  parameters {
    string(name: 'ENV',defaultValue: 'Dev')
    choice(name: 'ACTION',choices: ['apply','destroy'])
  }

  stages{
    stage('Execute'){
      steps{
        sh '''
          echo 'Env: ${params.ENV}'
          echo 'action: ${params.ACTION}'
        '''
      }
    }
  }
}